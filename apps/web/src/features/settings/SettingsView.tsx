import { useEffect, useState } from 'react';
import { MapPin, Save, Search } from 'lucide-react';
import type { WeatherLocation } from '@soren/shared';
import { api } from '../../api';

export function SettingsView(){
  const[settings,setSettings]=useState<Record<string,any>>({}),[persona,setPersona]=useState<Record<string,string>>({}),[mcp,setMcp]=useState<any>({servers:[],permissions:[]}),[activePersona,setActivePersona]=useState('core.md'),[saved,setSaved]=useState(false),[locationQuery,setLocationQuery]=useState(''),[locations,setLocations]=useState<WeatherLocation[]>([]),[searching,setSearching]=useState(false);
  useEffect(()=>{api<any>('/api/settings').then(data=>{setSettings(data.settings);setPersona(data.persona);if(data.settings.weatherLocation)setLocationQuery(data.settings.weatherLocation.name)});api<any>('/api/mcp').then(setMcp).catch(()=>{});},[]);
  const save=async()=>{await api('/api/settings',{method:'PUT',body:JSON.stringify({settings,persona})});setSaved(true);setTimeout(()=>setSaved(false),1800);};
  const findLocations=async()=>{if(locationQuery.trim().length<2)return;setSearching(true);try{setLocations((await api<{locations:WeatherLocation[]}>(`/api/weather/locations?q=${encodeURIComponent(locationQuery)}`)).locations);}finally{setSearching(false);}};
  const selected=settings.weatherLocation as WeatherLocation|undefined;
  return <section className="surface settings"><div className="surface-head"><div><h1>Settings</h1><p>模型、人格、记忆与工具权限。</p></div><button className="primary" onClick={save}><Save size={16}/>{saved?'已保存':'保存设置'}</button></div><div className="settings-grid"><article className="panel"><h2>Home 天气</h2><p className="setting-help">选择 Home 固定显示的城市，不读取设备定位。</p><div className="location-search"><MapPin size={16}/><input value={locationQuery} onChange={event=>setLocationQuery(event.target.value)} onKeyDown={event=>{if(event.key==='Enter')findLocations();}} placeholder="输入城市，例如：上海"/><button onClick={findLocations} disabled={searching}><Search size={15}/>{searching?'搜索中':'搜索'}</button></div>{selected&&<div className="selected-location">当前：{[selected.name,selected.admin1,selected.country].filter(Boolean).join(' · ')}</div>}{locations.length>0&&<div className="location-results">{locations.map(location=><button key={`${location.latitude}-${location.longitude}`} className={selected?.latitude===location.latitude&&selected?.longitude===location.longitude?'active':''} onClick={()=>{setSettings({...settings,weatherLocation:location});setLocationQuery(location.name);setLocations([]);}}><strong>{location.name}</strong><span>{[location.admin1,location.country].filter(Boolean).join(' · ')}</span></button>)}</div>}<h2>主动功能</h2><label className="toggle-row"><span><strong>全部暂停</strong><small>暂停主动消息与定时触发</small></span><input type="checkbox" checked={Boolean(settings.proactivePaused)} onChange={e=>setSettings({...settings,proactivePaused:e.target.checked})}/></label><label className="toggle-row"><span><strong>朋友圈生活</strong><small>有公开表达动机时，Soren 和固定好友可以自然更新</small></span><input type="checkbox" checked={settings.momentsLifeEnabled!==false} onChange={e=>setSettings({...settings,momentsLifeEnabled:e.target.checked})}/></label><h2>Persona</h2><div className="persona-tabs">{Object.keys(persona).map(name=><button className={activePersona===name?'active':''} onClick={()=>setActivePersona(name)} key={name}>{name.replace('.md','')}</button>)}</div><textarea className="persona-editor" value={persona[activePersona]||''} onChange={e=>setPersona({...persona,[activePersona]:e.target.value})}/></article><article className="panel"><h2>MCP Connections</h2>{mcp.servers?.map((server:any)=><div className="mcp-server" key={server.id}><div><strong>{server.name}</strong><span className={server.status?.connected?'connected':''}>{server.status?.connected?'已连接':'暂不可达'}</span></div>{(server.tools||[]).map((tool:any)=>{const current=mcp.permissions?.find((p:any)=>p.server===server.id&&p.tool===tool.name);return <label key={tool.name}><span>{tool.name}<small>{tool.description}</small></span><select value={current?.permission||'ask_each_time'} onChange={async e=>{const permission={server:server.id,tool:tool.name,permission:e.target.value,risk:/delete|archive|write|exec/i.test(tool.name)?'high':'low'};await api('/api/mcp/permissions',{method:'PUT',body:JSON.stringify(permission)});setMcp({...mcp,permissions:[...(mcp.permissions||[]).filter((p:any)=>!(p.server===server.id&&p.tool===tool.name)),permission]});}}><option value="always_allow">始终允许</option><option value="ask_each_time">每次询问</option><option value="disabled">停用</option></select></label>})}</div>)}</article></div></section>;
}
